import Store from '../store';

export default class ViewTool {
  private store: Store;

  constructor(store: Store) {
    this.store = store;
    this.init();
  }

  private init(): void {
    //
  }
}
