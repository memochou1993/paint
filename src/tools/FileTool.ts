import Store from '../store';

export default class FileTool {
  private store: Store;

  constructor(store: Store) {
    this.store = store;
    this.init();
  }

  private init(): void {
    //
  }
}
